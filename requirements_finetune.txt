# Core ML
torch>=2.5.0
torchvision>=0.20.0
transformers>=4.45.0
datasets>=3.0.0
huggingface_hub>=0.20.0

# Fine-tuning
trl>=0.12.0
peft>=0.13.0
accelerate>=1.0.0
bitsandbytes>=0.44.0

# Unsloth (fast LoRA training)
# NOTE: Install unsloth AFTER torch. Do NOT install torchao (causes torch.int1 conflict).
# If torchao gets installed as dependency, run: pip uninstall torchao -y
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# CUDA
xformers
nvidia-nvjitlink-cu12
nvidia-cuda-nvcc-cu12

# Tracking & Visualization
wandb>=0.16.0
matplotlib>=3.8.0
